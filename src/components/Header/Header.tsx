import { HeaderContainer, Logo } from "@src/components/Header/Header.styled";

export default function Header() {
  return (
    <HeaderContainer>
      <Logo>cokid</Logo>
    </HeaderContainer>
  );
}
